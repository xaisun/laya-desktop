# laya 桌面软件打包说明（独立 exe）

> 把 `C:\Users\sunxi\Desktop\启动 laya.bat`（依赖 venv 里的 Python）封装成**不依赖任何 Python 安装**的独立桌面软件。
> 成果：`Desktop\laya-desktop\` 整个文件夹 + `Desktop\启动 laya 桌面版.bat` 启动器。双击即用。

---

## 一、技术方案

**目标**：原 `启动 laya.bat` 只是调用 `venv\Scripts\python.exe server.py`，换台机器/删了 venv 就跑不起来。打包后把 Python 运行时、torch、transformers、laya 全部冻结进 exe，模型随附，做到真正独立。

**选型：PyInstaller `onedir`（单文件夹）**，而不是 onefile 或 pywebview：

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **onedir（采用）** | 启动快（torch 不重复解压）；结构清晰；模型/页面放 `_internal` 即随附 | 交付是一个文件夹（exe + `_internal` + `models`），不是单个文件 | ✅ 最适合本地 ML 应用 |
| onefile（单 exe） | 桌面上只有一个 `.exe` 图标，最"干净" | 每次启动把 torch(~800MB+)解压到 `%TEMP%`，慢 10–30s 且反复写临时盘；模型仍须外部 | ⚠️ 体验差，仅当你极度想要单文件时再用 |
| pywebview 原生窗口 | 操作台变成原生窗口（无浏览器地址栏），更像"软件" | 多一个依赖；Win 上依赖系统 Edge WebView2；打包更复杂 | 🔲 进阶可选，本次保持浏览器操作台 |

**架构**：exe 内含 Python3.13 + torch(CPU) + transformers + laya + huggingface_hub。运行即起一个 `http.server`（监听 `127.0.0.1:8811`，后台线程加载双模型），加载完浏览器自动打开操作台页面（同原 `server.py` 逻辑，零改动复用）。**模型 1.5GB 不进包**，随附在 `_internal/models`，随文件夹一起分发。

---

## 二、依赖配置

| 项 | 版本/路径 | 说明 |
|---|---|---|
| Python | 3.13.12（托管 venv） | `C:\Users\sunxi\.workbuddy\binaries\python\envs\laya` |
| PyInstaller | 6.22.3 | 在 laya venv 内 `python -m pip install pyinstaller`（**别 upgrade pip**，会触发本机 safe-delete 钩子批量删失败） |
| torch | 2.14.0+cpu | CPU 推理，不抢 LM Studio 的 16G 显存 |
| transformers | 5.17.0 | ModernBERT/mmBERT 的 tokenizer/model |
| laya | 0.3.5 | 决策引擎本体 |
| huggingface_hub | 1.32.0 | 仅本地加载，已关 telemetry |
| 模型 | `models/english` + `models/multilingual` | curl 直下（绕过 HF 缓存损坏），加载完全不联网 |

> 为什么不用系统 Python 3.11.9？本例**不需要 tkinter/GUI**（界面是浏览器），所以直接复用已装好 torch 的 3.13.12 托管 venv 即可。若要打包带 tkinter 的原生窗口 exe，才必须用系统 3.11.9（托管 3.13.12 缺 tcl）。

---

## 三、打包步骤

入口脚本 `make_desktop.py`（已落盘在 `laya-demo\`）核心参数：

```python
PyInstaller.__main__.run([
    "server.py",                 # 直接复用原服务，零改动
    "--name", "laya-desktop",
    "--onedir",                  # 单文件夹
    "--console",                 # 保留控制台看加载进度 / Ctrl+C 停止
    "--noconfirm",
    "--add-data", r"绝对路径\web;web",   # 操作台页面随包，必须绝对路径
    "--hidden-import", "torch",
    "--hidden-import", "transformers",
    "--hidden-import", "laya",
    "--hidden-import", "huggingface_hub",
    "--collect-all", "transformers",     # transformers 大量动态 import，全量收集兜底
    "--collect-all", "laya",
    "--collect-all", "huggingface_hub",
    "--exclude-module", "tkinter",       # 用不到，瘦身（unittest 千万别 exclude！）
])
```

**执行顺序**：
1. `python -m pip install pyinstaller`（在 laya venv）
2. `python make_desktop.py` → 产出 `build_out/dist/laya-desktop/`（含 `laya-desktop.exe` + `_internal/`）
3. 把 `models/` 整个移进 `build_out/dist/laya-desktop/_internal/models`（**关键**：onedir 下 `BASE` 解析到 `_internal`，模型必须在这里，不是 dist 根）
4. 验证：后台起 exe（`--no-browser`），轮询 `/api/status` 到 `ready`，跑一次 `/api/predict`，再 `/api/shutdown`

> ⚠️ 重建时注意：本机 safe-delete 钩子会拦截 PyInstaller 对旧 `dist` 的 `rmtree`（fail-closed 直接拒绝），导致重建报错。**做法：每次重建换一个新的 `distpath/workpath/specpath`（如 `build_out2`）**，避免删除旧目录。

---

## 四、关键事项（踩过的坑）

1. **冻结态 `BASE` 指向 `_internal`**：onedir 打包后主脚本运行在 `_internal` 下，所以 `os.path.dirname(__file__)` 解析到 `_internal`。`--add-data web` 也被放进 `_internal/web`。因此**模型必须放在 `_internal/models`**，否则报"模型目录缺失"。
2. **`--add-data` 用绝对路径**：相对路径会被 PyInstaller 按 specpath 解析，报 `Unable to find '.../build_out/web'`。用 `os.path.join(HERE,"web")` 绝对路径即可。
3. **不要 `--exclude-module unittest`**：torch/transformers 运行时会 `import unittest`，剔了直接 `ModuleNotFoundError: No module named 'unittest'`。本次已用把标准库 `unittest` 目录拷进 `_internal/unittest` 的快修解决，并已在 `make_desktop.py` 里移除该 exclude。
4. **验证时禁代理**：本机 Bash 会注入 `http_proxy`，挡住 `127.0.0.1` 回环（502）。验证客户端用 `urllib.build_opener(ProxyHandler({}))` 禁代理才能连上。
5. **桌面启动器编码**：`.bat` 必须**纯 ASCII + CRLF + 无 BOM**，带 UTF-8 BOM 会 `锘緻echo` 直接崩（返回 255）。中文提示交给 Python 打，bat 只做 ASCII 跳转。

---

## 五、潜在问题

- **体积大**：交付文件夹 ~2.0GB（exe 64MB + `_internal` 513MB + 模型 1.5GB）。属 ML 应用常态，移动/备份注意空间。
- **杀软误报**：PyInstaller 打包的 exe 常被 Windows Defender/管家误报。首次运行若被拦，需加白名单/允许。
- **端口占用**：默认 8811。若被占用，`server.py` 会提示并直接打开已有服务；要换端口需改 `server.py` 的 `PORT` 后重建。
- **onefile 慢**：若改用单文件 exe，每次启动解压 torch 到临时盘，慢且反复写盘，不推荐。
- **`noul`（是/否）中文不可靠**：laya 的 `noul` 类型对中文样本错判（"换供应商"被识别为低流失风险）。接企业工单分派前需补中文样本或改用 `choice/score` 型——这是模型能力短板，与打包无关。
- **迁移方式**：整个 `laya-desktop` 文件夹拷到任意 Windows 机器（同机直接剪切也行）即可运行，**不依赖 Python、不联网**。只需保证 `_internal/models` 完整。

---

## 六、部署与使用

- 已拷贝到桌面：`C:\Users\sunxi\Desktop\laya-desktop\`
- 启动器：`C:\Users\sunxi\Desktop\启动 laya 桌面版.bat`（双击即起 exe + 开浏览器操作台）
- 原 `启动 laya.bat`（走 venv）仍保留，确认新桌面版无误后可手动删除旧的那份。
- 停止：关 exe 控制台窗口，或点操作台上的"停止服务"（发 `/api/shutdown` 真退出，不留僵尸进程）。

**实测结果**（冻结 exe）：模型 37.8s 就绪；中文工单正确路由到 `multilingual`；`department=technical`(conf 0.956)、`urgency` 评分 1.94；单次推理 297ms；shutdown 1 秒内干净退出。✅
