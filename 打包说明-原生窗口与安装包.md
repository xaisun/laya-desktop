# laya 桌面软件 · 完整打包说明（原生窗口 + onefile 单 exe + 安装包）

> 目标：把 `启动 laya.bat`（stdlib http 服务 + 浏览器操作台）升级成
> **双击即开的独立桌面软件**：原生无边框窗口（pywebview） + 单个 exe + 7z 自解压安装包。

---

## 一、最终交付物

| 文件 | 说明 |
|------|------|
| `laya-desktop.exe` | onefile 单 exe（含 Python+torch+transformers+laya+pywebview），双击即开**原生窗口**，无需 Python |
| `models/` | 1.5GB 模型权重，**与 exe 同级**随附（不进包，原因见第六节） |
| `laya-desktop-setup.exe` | 7z 自解压安装包，双击→解压到 `%LOCALAPPDATA%\laya-desktop`→桌面建 `laya-launch.bat`→启动 |
| `laya-launch.bat` | 同目录启动 exe 的纯 ASCII 启动器（安装包会拷到桌面当快捷方式） |

---

## 二、技术方案总览

1. **复用后端**：`server.py` 的 stdlib `http.server`（端口 8811）和模型加载逻辑完全不动，
   只把它拆出可非阻塞调用的 `start_server()`。
2. **原生窗口**：新增 `app_native.py`，后台起 HTTP 服务 + 加载模型，再用 **pywebview**
   开一个原生窗口直接渲染 `http://127.0.0.1:8811/`。没有浏览器地址栏/边框，体验就是个本地 App。
3. **冻结**：PyInstaller `--onefile --windowed`，把 Python3.13 + torch(CPU) + transformers + laya
   + pywebview 全部打进单个 exe。
4. **模型外置**：1.5GB 权重不进包，运行时从 **exe 同级 `models/`** 读取（兼容 onefile 解压态
   `sys._MEIPASS` 与 exe 目录两种路径，见 `server._resolve_models_dir`）。
5. **安装包**：用本机已有的 `7z.sfx` 模块拼出自解压 exe，无需 NSIS/Inno。

### 为什么不用「onefile + 模型全打进包」？
1.5GB 模型打进 onefile → exe 约 2GB，**每次启动要把 2GB 解压到 `%TEMP%`**（1–3 分钟），
远超「启动慢」的合理范围。模型外置后：onefile exe ≈ 600MB，每次启动只解压 ~600MB（20–40s），
模型从同级文件夹直接加载。若你确实要「一个文件走天下」，把 `--add-data models;models` 加回
`make_desktop_onefile.py` 重新构建即可（代价是启动极慢）。

### 为什么 pywebview 而不是 Electron/CEF？
pywebview 在 Windows 复用系统 **WebView2**（Edge 内核），零额外浏览器二进制、体积小、
启动快；WebView2 运行时 Win11/装了 Edge 的机器自带。本机已确认 `EdgeWebView 153.x` 存在。

---

## 三、依赖配置

**虚拟环境**：`C:\Users\sunxi\.workbuddy\binaries\python\envs\laya`（Python 3.13，torch 2.x CPU）

新增依赖（相对 onedir 版）：
```
pywebview==6.2.1      # 原生窗口（Windows 后端 = WinForms + pythonnet/clr）
pythonnet==3.1.0      # clr 加载 .NET（WebView2 的 .NET 程序集靠它）
clr_loader==0.3.1     # pythonnet 的 CLR 宿主
proxy_tools / bottle  # pywebview 运行依赖
```
pywebview 自带 `webview/lib/` 下的 WebView2 .NET DLL（`Microsoft.Web.WebView2.Core.dll` 等）
和 `runtimes/win-x64/native/WebView2Loader.dll`，打包时由 `--collect-all webview` 一并收进包。

**PyInstaller 隐藏导入 / 收集**（见 `make_desktop_onefile.py`）：
```
--hidden-import webview pythonnet clr clr_loader proxy_tools bottle
--collect-all webview pythonnet transformers laya huggingface_hub
--exclude-module tkinter        # 用不到，瘦身
# 注意：绝不能 --exclude-module unittest（torch/transformers 运行时要用）
```

---

## 四、打包步骤

```bash
# 1) 装原生窗口依赖
python -m pip install pywebview

# 2) 写入口（已就绪）
#    app_native.py      —— 原生窗口版入口
#    server.py          —— 抽出 start_server()，模型目录兼容 exe 同级

# 3) 构建 onefile
python make_desktop_onefile.py
#    -> build_onefile/dist/laya-desktop.exe

# 4) 模型外置：把 models/ 放到 exe 同级
#    build_onefile/dist/ 下得到 laya-desktop.exe，再拷 models/ 进去即可分发
```

---

## 五、安装包制作（`laya-desktop-setup.exe`）

```bash
# 1) 准备分发包目录，里面含：laya-desktop.exe + models/ + laya-launch.bat + install_helper.bat
# 2) 用 7z 压成 .7z
"c:/Program Files/7-Zip/7z.exe" a -t7z laya.7z -r .\dist\*
# 3) 拼接自解压：7z.sfx + sfx_config.txt + laya.7z
copy /b "c:/Program Files/7-Zip/7z.sfx" + sfx_config.txt + laya.7z laya-desktop-setup.exe
```

`sfx_config.txt`（UTF-8 BOM）：
```
;!@Install@!UTF-8!
Title="laya 决策引擎 安装程序"
BeginPrompt="即将安装 laya 决策引擎（约 2GB），解压到安装包同级的 laya 文件夹。继续？"
InstallPath="laya"
RunProgram="install_helper.bat"
;!@InstallEnd@!
```
`install_helper.bat` 在解压后执行：把 `laya-launch.bat` 拷到桌面并启动程序。

> 用相对路径 `laya`（解压到安装包同级），不依赖 `%LOCALAPPDATA%` 展开、也不与桌面上旧的
> `laya-desktop`（onedir 版）文件夹重名。建议把 `laya-desktop-setup.exe` 放到非系统盘再运行。

---

## 六、关键注意事项与潜在问题

1. **onefile 冷启慢（已接受）**：每次启动把 ~600MB 运行时解压到 `%TEMP%\_MEIPASS`，
   再加载模型（30–60s），总计约 40–90s。onedir 版只有模型加载（~37s），更快。
2. **模型必须外置同级**：`server._resolve_models_dir` 先找 `exe 同级/models`，再找
   `_MEIPASS/models`。分发时 exe 与 models 文件夹必须在一起。
3. **WebView2 运行时缺失会建窗失败**：少数精简系统没有 WebView2。现象：窗口起不来 /
   报 `Microsoft.Web.WebView2.Core` 加载失败。修复：装 WebView2 Evergreen Runtime
   （https://developer.microsoft.com/zh-cn/microsoft-edge/webview2/）。
4. **pywebview 后端依赖必须收集全**：Windows 走 `WinForms + pythonnet/clr`，
   漏 `--collect-all webview` 会导致 `webview/lib` 里的 WebView2 .NET DLL 没进包 → 建窗失败。
5. **原生窗口无法在无头沙箱验证**：CI/服务器没有显示器，`webview.start()` 起不来。
   本环境只验证「`--no-window` 下 API 正常」，真实窗口渲染需在老板本机确认。
6. **杀软可能误报**：PyInstaller 打包的 exe 常被 Windows Defender 误判，首次运行加白名单。
7. **`.bat` 启动器必须纯 ASCII + CRLF + 无 BOM**：带 UTF-8 BOM 会 `'锘緻echo'` 直接崩。
   桌面快捷方式用 `.bat` 而非 `.lnk`（本机 `.lnk` 的 COM 创建被安全策略拦）。
8. **`shutdown` 用 `os._exit(0)`**：只停 `serve_forever` 会留半死 socket 挡下次启动。
9. **验证客户端要禁代理**：本机 Bash 注入 `http_proxy`，会挡 127.0.0.1 回环；
   `urllib.request.ProxyHandler({})` 解决（见 `verify_exe.py`）。
10. **`noul`（是/否）对中文不可靠**：这是模型短板，与打包无关；接中文工单分派前需补样本。

---

## 七、实测结果

- **onefile exe 体积**：209 MB（不含模型；模型外置 1.5GB）
- **安装包体积**：1.55 GB（`laya-desktop-setup.exe`，7z LZMA）
- **无头 API 验证（onefile + `--no-window`）**：
  - 模型就绪：37.2 s（外部 `models/` 正常加载，`models_ok=True`）
  - 单次推理：265 ms；中文工单正确路由到 `multilingual`（han 82%）
  - `shutdown`：1 秒内 `os._exit(0)` 干净退出 ✅
- **原生窗口渲染**：⚠️ 无头沙箱无法验证，需在老板本机双击确认
  （本机已确认 WebView2 运行时 `EdgeWebView 153.x` 存在、`import webview` 正常）

> onefile 冷启 = 运行时自解压到 `%TEMP%\_MEIPASS`（约 10–20s，杀软首扫会更久）
> + 模型加载 37s，合计约 50–90s。相比 onedir 版（仅模型加载 ~37s）慢，但换来单文件 + 安装包。

---

## 八、桌面交付物清单（本次新增）

| 路径 | 用途 |
|------|------|
| `Desktop\laya-onefile\laya-desktop.exe` | **onefile 便携版**：双击直接开原生窗口（需同级 `models/`） |
| `Desktop\laya-onefile\models\` | 1.5GB 权重（与 exe 同级） |
| `Desktop\laya-onefile\laya-launch.bat` | 同目录启动 exe（ASCII 启动器） |
| `Desktop\laya-desktop-setup.exe` | **安装包**：双击→解压到同级 `laya-desktop\`→桌面建 `laya-launch.bat`→启动 |
| `laya-demo\打包说明-原生窗口与安装包.md` | 本说明 |
| `laya-demo\make_desktop_onefile.py` / `app_native.py` / `make_installer.py` | 可复跑的构建脚本 |

> 旧的 `Desktop\laya-desktop\`（onedir 浏览器版）仍可用；如确认 onefile 版无误，旧那份可手动删除（我不擅自删个人文件）。
